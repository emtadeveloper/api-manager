"use client";
import { FormInstance } from "antd";
import React from "react";
interface Props {
  form: FormInstance;
}
const InitAuth = ({ form: _form }: Props) => {
  return <div>InitAuth</div>;
};

export default InitAuth;
