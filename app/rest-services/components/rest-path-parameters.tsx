import { Empty } from "antd";

const RestPathParameters = () => {
  return (
    <div className="app-empty-block">
      <Empty description="پارامترهای مسیر (Path) به‌زودی" />
    </div>
  );
};

export default RestPathParameters;
